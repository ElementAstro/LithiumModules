#ifndef LITHIUM_IMAGE_CIMG_DRAW_HPP
#define LITHIUM_IMAGE_CIMG_DRAW_HPP

#include <string>

bool drawStarImage(const std::string &filename, int &star_index, double &total_hfd, bool is_save, bool is_plot);

#endif